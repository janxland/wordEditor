## 连接器合并（2026-09-06）
- `~/.workbuddy/mcp.json` 移除 `hutb-style`，只保留 `wordeditor` 一个连接器。
- `style_core.py` 从 skill `hutb-docx-export` 迁入 `services/api-python/`（与 direct.py 同级）；custom_styles 移到 `config/custom_styles/`；skill 内 `mcp_style_server.py`、`style_core.py`、空 `custom_styles/` 已删除，SKILL.md 同步更新。
- `mcp_server.py` 现提供 7 个工具：templates / build_docx / apply_style / save_style / list_styles / save_template / remove_template。
- 冗余扫描结论：legacy/macros 与 macros 有一份完全相同的 .bas（legacy 有 README 说明属有意保留）；services/api-node 是与 api-python 平行的 Node 后端（API parity），前端实际用哪个未确认，未动。
- 注意：mcp.json 改后需重启会话/Trust 才生效。
